import { Routes } from '@angular/router';
import { GithubExplorerComponent } from './features/github-explorer/github-explorer.component';

export const routes: Routes = [{ path: '', component: GithubExplorerComponent }];
